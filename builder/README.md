# Builder settings

This folder keeps the builder settings in one easy-to-find place.

If you want builder mode enabled on a local machine:

1. Open your local `.env` file in the project root.
2. Copy the values from `builder.env.example` into `.env`.
3. Restart the app.

The actual builder values stay local and are not committed as secrets.
